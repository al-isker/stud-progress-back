# Соглашения

## Форматирование и самопроверка

После **любых** изменений в коде обязательно запускать:

```bash
npx prettier . --write   # форматирование
npx tsc --noemit         # проверка типов
```

Все шаги должны проходить без ошибок до завершения работы.
